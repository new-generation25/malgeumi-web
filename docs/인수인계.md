# 말그미극단 홈페이지 인수인계

최종 갱신: 2026-08-09

---

## 1. 한눈에 보기

말그미극단 홈페이지입니다. **정적 HTML 3장**으로 되어 있고 빌드 과정이 없습니다.
파일을 고쳐서 git에 push하면 Vercel이 자동 배포합니다.

| 항목 | 값 |
|---|---|
| 라이브 주소 | https://malgeumi-play.vercel.app |
| GitHub | https://github.com/new-generation25/malgeumi-web |
| 로컬 폴더 | `F:\projects\malgeumi-web` |
| 배포 | Vercel (GitHub `main` 브랜치 push 시 자동) |
| 빌드 | **없음.** 프레임워크·번들러·npm 없음 |

> **중요:** 이 사이트는 **말그미극단**의 홈페이지입니다. 회현동소극장은 공연장으로만 등장합니다.
> 말그미극단 대표는 **김미경**, 회현동소극장 대표는 **이정화**입니다. 소극장 대표 이름은 이 사이트에 넣지 않습니다.

---

## 2. 페이지 구성

```
index.html      말그미극단 메인          66KB
king.html       「King받은 이도」 공연     36KB
theater.html    회현동 소극장 소개        31KB
```

### 페이지 간 연결

```
index.html ──(네비 · 카드 · 지도)──▶ theater.html ──(푸터)──▶ index.html
    │
    └──(공연 카드)──▶ king.html ──(네비)──▶ index.html
```

세 페이지 모두 상단에 **같은 네비게이션**이 있습니다. 메뉴 항목은
`홈으로 · 활동 철학 · 공연 안내 · 지난 활동 · 소극장 · 소식 구독` 입니다.

### index.html 섹션 순서

1. 히어로
2. `#philosophy` 활동 철학 — 극단 소개 문장 + 카드 3장 + 전문예술단체 지정 배지
3. `#project` 공연 안내 (카드 그리드 — 현재 King 1장)
4. `#archive` 지난 활동 (가로 스크롤 카드 5장 — 7-4 참고)
5. `#space` 회현동 소극장 (요약 + theater.html 링크 + 네이버 지도 길찾기)
6. `#contact` 소식 구독 → 구글 시트
7. 푸터

---

## 3. 이미지

이미지는 **`images/` 한 폴더**에 모여 있습니다. 세 페이지가 모두 여기를 씁니다.

(2026-08-08 이전에는 king.html만 `img/`라는 별도 폴더를 썼습니다. 합쳤습니다.
king.html의 `<img>`에는 전부 `onerror`로 자기 자신을 지우는 핸들러가 붙어 있어,
**경로를 틀리면 에러 없이 이미지가 조용히 사라집니다.** 고친 뒤에는 눈으로 확인하세요.)

### 미사용 이미지

아래는 어느 페이지에서도 참조하지 않습니다. 지워도 사이트에 영향이 없습니다. 합쳐서 약 6MB입니다.

- `images/chobi.png` (5.2MB) — 초비 원본
- `images/chobi3.png` (554KB) — king.html 주석에서 이름만 언급됩니다
- `images/chobi2.png` (174KB) · `images/uri.png` (149KB) · `images/gongzon.png` (295KB)
  — 지난 활동 카드에 쓰던 일러스트. 2026-08-08에 실제 포스터·공연사진으로 교체되며 빠졌습니다

### 지난 활동에 쓰는 이미지

`work-*.jpg` 5장은 **4:5 세로**로 통일돼 있습니다 (900×1125).
카드 CSS가 `aspect-ratio: 4/5`라 다른 비율을 넣으면 잘립니다. 새로 추가할 때도 4:5로 맞추세요.

```bash
# 세로 포스터
ffmpeg -i 포스터.jpg -vf "crop=600:750:0:40,scale=900:1125:flags=lanczos" -q:v 3 images/work-이름.jpg
# 가로 공연사진 (가운데 피사체 기준으로 x 오프셋을 조절)
ffmpeg -i 사진.jpg -vf "crop=516:645:186:0,scale=900:1125:flags=lanczos" -q:v 3 images/work-이름.jpg
```

> ⚠ **모바일에서는 이 카드 이미지가 `aspect-ratio: 1/1`로 바뀝니다.** 4:5 그대로 두면 포스터에 본문까지
> 붙어 카드가 705px이 되어 한 화면에 안 들어갑니다. 세로를 낮춰 532px로 맞춰둔 것입니다.

`images/cert-252.jpg`는 전문예술단체 지정서입니다.
**원본에 대표자 생년월일이 찍혀 있어 흰 상자로 가린 판본**입니다. 원본으로 교체하지 마세요.

### 사진에 사람 얼굴이 나올 때

아이들 얼굴이 알아볼 수 있게 나오는 사진은 **보호자 동의 없이 올리지 않습니다.**
지금 쓰는 사진 중 `work-geuraeyo.jpg`는 아이들이 뒤통수만 보이도록 고른 것입니다.
좋은 커튼콜 사진이 원자료에 있지만 같은 이유로 넣지 않았습니다 (9절).

---

## 4. 자주 하는 작업

### 4-1. 새 공연 추가하기

`index.html`의 `#project` 섹션에서 `.show-card` 블록을 복사해 붙입니다.
그리드가 flex라 카드가 1개든 3개든 알아서 배치됩니다.

```html
<a class="show-card" href="공연페이지.html">
  <div class="show-card-thumb">
    <img src="images/포스터.jpg" alt="공연 포스터" loading="lazy" width="900" height="1118">
    <span class="show-card-badge">🎫 예매 중</span>
  </div>
  <div class="show-card-body">
    <h3>공연 제목</h3>
    <p class="show-card-meta"><i class="fa-regular fa-calendar"></i> 날짜 · 장소</p>
    <p class="show-card-desc">한 줄 소개</p>
    <span class="show-card-cta">자세히 보기 <i class="fa-solid fa-arrow-right"></i></span>
  </div>
</a>
```

- 썸네일 비율은 **4:5(세로)** 입니다. 세로 포스터가 잘리지 않게 맞춰둔 값입니다.
- 배지에 `is-upcoming` 클래스를 붙이면 흰 바탕(예정), 없으면 노란 바탕(예매 중)입니다.
- **끝난 공연은 카드를 지웁니다.** 진행하지 않는 공연을 남겨두면 어느 걸 예매해야 할지 헷갈립니다.

### 4-2. 이미지 넣기

원본을 그대로 올리지 말고 아래를 지킵니다.

```bash
# 크기 조정 + JPEG 변환 (ffmpeg 설치돼 있음)
ffmpeg -i 원본.png -vf "scale=1200:-2:flags=lanczos" -q:v 3 images/결과.jpg

# 특정 영역만 잘라내기 (crop=폭:높이:x:y)
ffmpeg -i 원본.png -vf "crop=900:1125:100:50,scale=1200:1500:flags=lanczos" -q:v 3 images/결과.jpg
```

화면 밖 이미지에는 `loading="lazy"`를, 첫 화면 이미지에는 `fetchpriority="high"`를 붙입니다.

로딩 중 화면이 튀지 않게 **자리를 미리 잡아둬야** 합니다. 페이지마다 방법이 다릅니다.

| 페이지 | 방법 |
|---|---|
| index.html, theater.html | `<img>`에 `width`/`height` 속성 |
| **king.html** | CSS `aspect-ratio` (`figure img{aspect-ratio:3/2}` 등) |

king.html의 `<img>`에는 `width`/`height`가 없습니다. **일부러 그런 것이니 넣지 마세요.**
CSS가 이미 비율을 잡고 있어서, 속성을 추가하면 오히려 충돌합니다.

> ⚠ **함정:** `width`/`height` 속성을 쓰는 페이지는 CSS에 `img { height: auto }`가 반드시 있어야 합니다.
> 없으면 브라우저가 속성의 높이값을 그대로 적용해 이미지가 세로로 늘어납니다.
> index.html(82행)과 theater.html(29행)에 들어 있습니다. 새 페이지를 만들 때 빠뜨리기 쉽습니다.

### 4-3. 연락처 바꾸기

전화·이메일·인스타그램·블로그가 여러 곳에 흩어져 있습니다. 바꿀 때 전부 찾으세요.

```bash
grep -rn "055-339-9110\|hoehyeon.theater@gmail.com\|instagram.com/hoehyeon\|blog.naver.com" *.html
```

### 4-4. 배포

```bash
git add -A
git commit -m "설명"
git push origin main
```

push하면 Vercel이 1~2분 안에 자동 배포합니다. 별도 배포 명령이 없습니다.

---

## 5. 도메인 — 가장 자주 사고가 난 부분

**Vercel의 "프로젝트 이름"과 "도메인"은 별개입니다.** 프로젝트 이름을 바꿔도 도메인은 따라오지 않습니다.

도메인 변경 방법: Vercel 대시보드 → 프로젝트 → **Settings → Domains** → 새 주소 Add

### 도메인을 바꾸면 반드시 함께 고쳐야 하는 곳

절대주소가 하드코딩된 자리가 **9곳** 있습니다. 안 고치면 **카톡·인스타 공유 시 썸네일이 깨집니다.**

```bash
grep -rn "malgeumi-play.vercel.app" *.html
```

| 파일 | 항목 |
|---|---|
| index.html | `canonical`, `og:url`, `og:image` |
| king.html | `canonical`, `og:url`, `og:image` |
| theater.html | `canonical`, `og:url`, `og:image` |

기존 도메인을 **삭제하면 그 주소로 공유된 링크는 전부 죽습니다.** 새 주소를 추가한 뒤
기존 주소를 함께 두는 편이 안전합니다.

> 참고: 카카오톡은 OG 정보를 캐싱합니다. 깨진 채로 한 번 공유되면 고친 뒤에도 한동안 그대로 뜹니다.

---

## 6. 알아둘 제약

### 소식 구독 폼 — 구글 시트로 들어갑니다

`index.html`의 구독 폼은 입력 내용을 **구글 시트에 한 줄씩 기록**합니다.
홈페이지가 시트에 직접 쓰는 게 아니라, 시트에 붙어 있는 **Apps Script 웹앱**이 중간에서 받아 적습니다.

```
방문자 → index.html 구독 폼 → Apps Script 웹앱 → 구글 시트
                (fetch POST)      (시트 주인 권한으로 실행)
```

| 항목 | 값 |
|---|---|
| 시트 | 「말그미극단 소식 구독자 명단」 (`hoehyeon.theater@gmail.com` 소유) |
| 시트 주소 | https://docs.google.com/spreadsheets/d/1S8vb0Thx2P7-Wh1CQxV8TrU1zntcjrbitF27YJrczEw/edit |
| 스크립트 | [docs/구독폼-AppsScript.gs](구독폼-AppsScript.gs) — 배포된 것과 같은 내용을 보관 |
| 웹앱 주소 | `index.html`의 `SHEET_ENDPOINT` 상수 |

**시트는 비공개로 두세요.** 이름과 휴대전화 번호가 쌓입니다. 공유하지 않아도 기록은 정상 동작합니다 —
스크립트가 시트 주인 권한으로 돌기 때문입니다.

#### 스크립트를 고쳐야 할 때

`.gs` 파일만 고치면 **반영되지 않습니다.** 시트 → 확장 프로그램 → Apps Script 에서 고친 뒤
**배포 → 배포 관리 → 연필(수정) → 버전: 새 버전 → 배포** 해야 합니다.
이렇게 하면 웹앱 주소는 그대로 유지됩니다. "새 배포"를 누르면 **주소가 바뀌어**
`index.html`의 `SHEET_ENDPOINT`도 함께 고쳐야 합니다.

#### 주소가 비어 있으면

`SHEET_ENDPOINT`가 빈 문자열이면 **예전 방식(메일 앱 열기)으로 자동 전환**됩니다.
웹앱을 다시 배포하는 동안 사이트가 멈추지 않게 해둔 장치입니다.

#### 신청이 끝나면 팝업이 뜹니다

`<dialog id="subscribeDone">`이 화면 가운데에 "구독이 완료되었습니다"를 띄웁니다.
확인 버튼·배경 클릭·Esc로 닫힙니다. `<dialog>`를 못 쓰는 오래된 브라우저에서는
폼 아래 초록 글씨로 대신 알립니다.

> ⚠ **버튼 상태 복구는 `finally`에 있습니다.** 예전에 성공 경로에서만 빠져 있어
> 신청은 정상으로 들어가는데 버튼이 '보내는 중…'에 멈춰 있었습니다. 옮기지 마세요.

> ⚠ **팝업 위치**: 맨 위 `* { margin: 0 }`이 `dialog`의 기본 가운데 정렬(`margin: auto`)을
> 덮어버립니다. `.modal`에서 `margin: auto`로 되돌려 놨습니다. 지우면 팝업이 화면
> 좌상단에 붙습니다.

### 개인정보

구독 폼이 **휴대전화 번호를 받습니다.** 수집·이용 **필수 동의 체크박스**가 있고,
체크하지 않으면 제출이 막힙니다. 스크립트도 `consent=y`가 아닌 요청은 **기록하지 않습니다** (이중 방어).

동의 여부와 **서버 기준 시각**이 시트에 함께 남습니다. 방문자가 고칠 수 없는 기록이라
개인정보보호법이 요구하는 동의 입증 자료로 쓸 수 있습니다.

문자 발송을 시작하면 동의와 별개로 아래도 지켜야 합니다 (정보통신망법).

- 문자 맨 앞에 **(광고)** 표기
- 무료 수신거부 수단 안내
- **21시~08시 발송 금지** (야간 수신동의를 따로 받지 않았다면)
- **2년마다** 수신동의 여부 재확인

동의 문구를 고칠 때는 `index.html`의 `.consent-detail` 문단과 위 문서를 함께 고치세요.

### 실존 인물 이미지

`king.html`의 이윤재(한뫼 이윤재, 1888~1943) 이미지는 **AI로 생성한 일러스트**입니다.
원래 기획 주석에는 "실존 인물, AI로 얼굴을 만들지 말 것 / 사료 사진 사용"이라고 적혀 있었습니다.
`alt`에 "(일러스트)"를 명시해뒀지만, **문제 제기 소지가 있는 항목**입니다.
공공누리 개방 사료 사진으로 교체하는 편이 안전합니다.

세종은 어진 재현 금지 때문에 **이미지 없이 텍스트만** 나갑니다. 의도된 상태입니다.

---

## 7. 디자인 규칙

### 네비게이션 — 세 페이지 완전 동일

색만 페이지 팔레트를 따르고 **크기·간격·위치는 픽셀 단위로 같습니다.**

```
패딩 16px 32px · 로고 24px/800 · 링크 간격 32px · 링크 15.2px
CTA 11.2px 28.8px, radius 9999px · 바 높이 81px
```

네비에는 **공통 시스템 폰트**를 씁니다. 페이지마다 본문 폰트가 달라(Noto Sans KR /
IBM Plex Sans KR / Pretendard) 같은 글자도 폭이 달라지기 때문입니다.
**네비 폰트를 페이지 폰트로 바꾸면 세 페이지의 메뉴 위치가 어긋납니다.**

모바일(768px 이하)에서는 햄버거 토글로 바뀝니다. 세 페이지 모두 같은 JS가 들어 있습니다.

### 글의 말투 — 구체적으로, 담백하게

두 페이지 모두 **자료에 적힌 사실을 그대로 쓰는 말투**로 맞춰져 있습니다.
"특별한 무대를 만들어갑니다" "핵심 가치" "발자취" "문화 숨구멍" 같은 상투구는 2026-08-09에 걷어냈습니다.

새 문구를 쓸 때 기준:

| 피할 것 | 대신 |
|---|---|
| 하이퍼-로컬, 핵심 가치, 발자취 | 함안 초등학교 열한 곳, 여든 개의 의자 |
| 꿈이 피어납니다, 숨구멍 | 낡은 건물 4층, 무대와 첫 줄 사이 몇 걸음 |
| 만나실 수 있는, 선사합니다 | 예매할 수 있는, 보내드립니다 |

숫자·지명·연도가 들어가면 대체로 좋은 문장입니다. 형용사가 많으면 다시 보세요.

### theater.html 연혁 — 제작 주체를 반드시 밝힙니다

이 극장에는 **말그미극단 · 극단 해연 · 회현동극장** 세 주체의 공연이 섞여 올라갑니다.
연혁의 각 작품에 `<span class="by">`로 제작 주체를 답니다. 빼면 극장이 한 단체 전용처럼 보이고,
대관 유치에도 불리합니다.

### 본문 팔레트는 페이지마다 다릅니다 (의도된 것)

| 페이지 | 배경 | 강조색 | 성격 |
|---|---|---|---|
| index | `#F0F9FF` 밝은 하늘 | `#FFD700` | 밝고 친근 |
| king | `#0E1319` 어두운 잉크 | `#E8B54B` | 극의 톤 |
| theater | `#0F0D0B` 어두운 갈색 | `#E0AB60` | 아카이브 |

공연별로 자기 아트디렉션을 갖는 것은 공연예술 웹에서 일반적입니다.
브랜드는 **로고·네비·노랑~금색 계열 강조색**이 잇습니다.

### 지난 활동 가로 스크롤 — 건드릴 때 주의

`#archive`는 옆으로 넘겨 보는 목록입니다. **옆에 더 있다는 걸 알리는 장치가 셋** 들어 있고,
하나라도 빠지면 방문자가 카드가 더 있는 줄 모릅니다.

| 장치 | 어디에 |
|---|---|
| 다음 카드가 살짝 걸침 | 모바일에서 카드 `74vw`, 스크롤러에 `margin-inline: -2rem` |
| 양끝 페이드 | `.archive-viewport::before/::after` + JS가 `can-prev`/`can-next` 토글 |
| 좌우 화살표 | `.archive-nav` (모바일에서는 포스터를 가려 숨김. 대신 안내 문구 한 줄) |

주의할 점:

- **`margin-inline: -2rem`이 섹션 밖으로 삐져나가 페이지에 가로 스크롤을 만듭니다.**
  모바일 미디어쿼리의 `#archive { overflow-x: hidden }`이 그걸 잘라줍니다. 지우지 마세요.
- 화살표 클릭은 `scrollBy`에 **안전장치**가 붙어 있습니다. `scroll-snap` 컨테이너에서 smooth
  스크롤을 취소하는 브라우저가 있어서, 350ms 뒤에도 안 움직였으면 그냥 옮깁니다.
- 화살표 상태는 `scroll` 이벤트뿐 아니라 **버튼 쪽에서도 갱신**합니다. 한쪽만 믿으면 화살표가
  끝에 닿아도 안 사라집니다.

### 지도 — 네이버 Dynamic Map

`#space`에 **진짜 네이버 지도**가 들어갑니다. 끌어서 움직이고 확대·축소됩니다.
오른쪽 아래 `길찾기` 버튼을 누르면 네이버 지도 플레이스로 나갑니다.

| 항목 | 값 |
|---|---|
| 콘솔 Application | `malgeumi-web` (네이버 클라우드 플랫폼) |
| Client ID | `ywa9tndcag` — `index.html`의 `NAVER_KEY` 상수 |
| 좌표 | `35.232767, 128.8744441` — `index.html`의 `SPOT` 상수 |
| 플레이스 | https://map.naver.com/p/entry/place/1044803603 |
| 콘솔 | https://console.ncloud.com/maps/subscription |

**비용은 들지 않습니다.** Dynamic Map 무료 이용량이 월 600만 건이고 계정 단위로 공유됩니다.
같은 계정에 `bonghwang-memories` 등 다른 앱이 있지만 키는 앱마다 따로입니다.

> ⚠ **도메인이 등록된 곳에서만 뜹니다.** 콘솔의 이 Application → 서비스 환경 등록 →
> Web 서비스 URL에 `https://malgeumi-play.vercel.app`과 `http://localhost:8765`가 들어 있습니다.
> **도메인을 바꾸면 여기도 추가해야 합니다.** 안 하면 `navermap_authFailure`가 나고 지도가 안 뜹니다.

#### 안 떠도 사이트는 안 깨집니다

키가 틀리거나 도메인이 안 맞거나 네이버가 죽으면, **예전 지도 이미지(`images/map.jpg`)가
그대로 남고** 누르면 네이버 지도로 나갑니다. `.map-box`에 `is-live`가 붙어야 지도로 바뀝니다.

#### 좌표를 다시 구해야 한다면

플레이스 페이지에는 **주변 장소들의 좌표가 여럿 섞여 있습니다.** 아무거나 짝지으면 엉뚱한 데
찍힙니다(처음에 수로왕릉역에 찍혔습니다). 아래처럼 극장 자신의 `coordinate` 항목만 뽑으세요.
`x`가 경도, `y`가 위도입니다.

```bash
curl -s -A "Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)" \
  "https://m.place.naver.com/place/1044803603/home" \
  | grep -oE '"coordinate":\{[^}]{0,120}'
```

구한 좌표는 반드시 역지오코딩으로 검산하세요. **김해도서관**이 나와야 맞습니다.

#### 마커는 핀이 아니라 이름표입니다

기본 핀을 쓰면 확대했을 때 그 자리의 다른 상호에 묻힙니다. 그래서 `.map-pin` 클래스를 쓴
HTML 이름표("회현동 소극장")를 마커 아이콘으로 넣습니다.

**`.map-pin` CSS를 지우면 마커가 맨 글씨로 나옵니다.** 이 규칙은 지도 안에 심어지는
요소를 꾸미는 것이라, 페이지 어디에서도 쓰지 않는 것처럼 보여도 지우면 안 됩니다.

#### 손볼 때 걸린 두 가지 함정

- **지도를 `display:none` 상태에서 만들면 빈 화면이 됩니다.** 컨테이너 크기가 0이라서입니다.
  그래서 `is-live`를 먼저 붙여 보이게 한 뒤 `new naver.maps.Map(...)`을 부릅니다. 순서를 바꾸지 마세요.
- **네이버 API가 컨테이너의 `position`을 `relative`로 덮어씁니다.** `.map-live`에 `absolute; inset:0`으로
  크기를 주면 높이가 0이 됩니다. `width/height: 100%`로 지정해야 합니다.

스크립트는 **지도가 화면에 들어올 때만** 불러옵니다(IntersectionObserver). 아래까지 안 내려가는
방문자에게는 요청 자체가 나가지 않습니다.

`images/map.jpg`는 이제 대체용으로만 쓰입니다.

### 구독 폼 CSS — 건드릴 때 주의

`index.html`의 폼 규칙은 `.contact-form input:not([type="checkbox"])` 로 시작합니다.
`:not()`을 지우면 동의 체크박스가 `padding: 1rem`을 물려받아 **거대한 상자로 렌더됩니다.**
폼에 새 `input` 규칙을 추가할 때도 체크박스를 함께 제외했는지 확인하세요.

체크박스에 `color-scheme: light`가 걸려 있습니다. 지우면 **OS 다크 모드에서 체크박스가 보이지 않습니다**
(어두운 체크박스가 짙은 남색 배경 위에 놓임). `appearance: none`으로 커스텀 디자인하는 것도 금물입니다 —
크롬이 아무 메시지 없이 제출을 거부해, 버튼을 눌러도 아무 일이 일어나지 않게 됩니다.

---

## 8. 관련 저장소

| 저장소 | 내용 | 배포 |
|---|---|---|
| `new-generation25/malgeumi-web` | **이 프로젝트** | https://malgeumi-play.vercel.app |
| `new-generation25/test-page` | AI 수업 자료 (대시보드·실습본) | https://test-page-sandy.vercel.app |

> ⚠ `test-page` 저장소는 **이름을 바꾸면 안 됩니다.** 그 안의 `dashboard_step3.html`이
> `github.com/new-generation25/test-page/raw/main/ppt/...` 를 하드코딩해 PPT를 불러옵니다.

로컬에 `F:\projects\malgmi-web` 폴더가 남아 있으면 병합 후 잔재입니다. 삭제해도 됩니다.

---

## 9. 남은 작업

| 항목 | 내용 |
|---|---|
| **활동철학 카드 사진** | `#philosophy` 카드 3장이 아직 이모지(🧸🏫📍)입니다. 대표님이 사진을 새로 준비해 넣기로 했습니다. 넣을 때 카드 구조를 사진용으로 바꿔야 합니다 |
| 미수록 작품 | 「화포천 사람들」(2022.3.1) 「짹투더가야」(2024.10)는 사진이 없어 지난 활동에 빠졌습니다 |
| 커튼콜 사진 | 원자료에 좋은 커튼콜 사진이 있지만 **아이들 얼굴이 그대로 나옵니다.** 보호자 동의를 받으면 히어로에 쓸 수 있습니다 |
| 〈버지니아 그레이의 초상〉 | 소극장 자료에 "공연 시기 확인 필요"로 적혀 있어 연혁에 넣지 않았습니다 |
| 사람들 소개 | 원자료에 단원 6명 소개가 있습니다. 홈페이지가 길어져 넣지 않았습니다. 필요하면 `about.html`을 따로 만드는 편이 낫습니다 |
| 실적 숫자 | 원자료의 "39회 · 1,771명 · 11곳"은 **2022년 상반기 기준**이라 넣지 않았습니다. 다시 집계하면 쓸 수 있습니다 |
| 개인정보 보유 기간 | 동의문에 보유 기간을 적지 않았습니다. 명단 관리 주기가 정해지면 `.consent-detail`에 한 줄 추가 |
| 이윤재 이미지 | AI 일러스트 → 사료 사진 교체 검토 |
| 미사용 이미지 | 약 6MB. 3절 참고 |
| 소셜 계정 | 페이스북·유튜브 계정을 만들면 세 페이지 푸터에 추가 (현재 인스타·블로그만) |

### 지난 활동 카드가 8장을 넘어가면

지금은 5장이라 가로 스크롤로 충분합니다. **8장을 넘으면** 옆으로 계속 넘기는 게 불편해집니다.
그때는 `about.html`을 만들어 전체 공연 이력·연혁·단원 소개를 몰아넣고,
홈페이지에는 대표작 5장 + "전체 보기" 링크만 두는 편이 낫습니다.

**연혁을 index.html에 넣지는 마세요.** 지난 활동 카드와 내용이 대부분 겹쳐 같은 공연을 두 번
나열하게 됩니다. 연혁은 theater.html에만 둡니다.

---

## 10. 연락처 (사이트에 실린 정보)

| 항목 | 값 |
|---|---|
| 대표 | 김미경 |
| 주소 | 경남 김해시 분성로 259, 4층 |
| 전화 | 055-339-9110 |
| 이메일 | hoehyeon.theater@gmail.com |
| 인스타그램 | https://www.instagram.com/hoehyeon.theater/ |
| 네이버 블로그 | https://blog.naver.com/jhoehyeon707 |

---

## 11. 사이트가 기대는 외부 서비스

사이트 밖에 있어서 **여기 코드만 봐서는 안 보이는 것들**입니다. 하나라도 죽으면 해당 기능이 멈춥니다.

| 서비스 | 쓰는 곳 | 죽으면 |
|---|---|---|
| **Vercel** | 배포 전체 | 사이트가 안 뜹니다 |
| **Apps Script 웹앱** | index.html 구독 폼 | 신청이 시트에 안 쌓입니다. `SHEET_ENDPOINT`를 비우면 메일 앱 방식으로 되돌아갑니다 |
| **구글 시트** | 구독자 명단 | 위와 같음 |
| **구글 폼** `forms.gle/BvXD3RBAWeCckMeQ7` | king.html 예매 버튼 | 예매 신청을 못 받습니다 |
| **네이버 Maps API** (Application `malgeumi-web`) | index.html 지도 | 지도가 안 뜨고 대체 이미지로 돌아갑니다 |
| **네이버 지도** 플레이스 `1044803603` | index.html 길찾기 버튼 | 길찾기 링크가 깨집니다 |
| Google Fonts · Font Awesome · Pretendard CDN | 세 페이지 | 글꼴·아이콘이 기본값으로 바뀝니다 (내용은 보입니다) |

계정은 모두 **`hoehyeon.theater@gmail.com`** 과 **`socialceos@gmail.com`** 에 걸려 있습니다.
담당자가 바뀌면 이 두 계정의 접근 권한부터 정리하세요.

---

## 12. 이 문서를 고칠 때

코드를 고치면 이 문서도 같이 고칩니다. 특히 아래를 바꿨다면 반드시 확인하세요.

| 바꾼 것 | 함께 볼 절 |
|---|---|
| 이미지 추가·교체 | 3 (미사용 목록, 4:5 규칙) |
| 도메인 | 5 (절대주소 9곳) |
| 구독 폼·Apps Script | 6, 11 |
| 카피 문구 | 7 (말투 기준) |
| 가로 스크롤·지도 | 7 |
| 새 외부 서비스 연결 | 11 |

문서와 코드가 어긋나면 **다음 사람이 문서를 안 믿게 됩니다.** 그때부터 이 문서는 쓸모가 없어집니다.
